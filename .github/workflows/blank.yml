name: Custom VM Script

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run script
        run: |
          # 在這裡執行你的腳本指令，例如：
          bash /path/to/your/script.sh
